# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
# Only ARM architectures for mobile devices (x86 removed for size optimization)
reactNativeArchitectures=armeabi-v7a,arm64-v8a

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

# Whether the app is configured to use edge-to-edge via the app config or `react-native-edge-to-edge` plugin
expo.edgeToEdgeEnabled=true

# Support for 16 KB page size (required for Android 15+ and Google Play Store)
# The ensure_16k_page.py script automatically aligns native libraries during build
# CRITICAL: Native libraries MUST be uncompressed for 16 KB alignment
# AGP 8.1+ automatically uses uncompressed libraries (default is true)
# Note: android.bundle.enableUncompressedNativeLibs was removed in AGP 8.1+
# Native libraries are uncompressed by default in AGP 8.1+
android.enable16KbPageSizeSupport=true

# Enable ProGuard/R8 for code obfuscation, minification, and size reduction
# R8 is the default code shrinker for Android (replaces ProGuard)
android.enableProguardInReleaseBuilds=true

# Enable resource shrinking to remove unused resources
android.enableShrinkResourcesInReleaseBuilds=true

# Enable R8 full mode for maximum optimization
android.enableR8.fullMode=true

# Additional R8 optimizations for aggressive code shrinking
android.enableR8.optimize=true
android.enableR8.optimizeClasses=true
android.enableR8.optimizeResources=true

# Additional size optimizations
# Disable unused resources in debug builds (already enabled in release via shrinkResources)
android.enableResourceShrinking=true

# Enable code shrinking
android.enableCodeShrinking=true

# Optimize PNG compression
android.enablePngCrunchInReleaseBuilds=true

# Note: android.disableAutomaticComponentCreation was removed in AGP 8.0
# Component creation is now handled automatically by the build system